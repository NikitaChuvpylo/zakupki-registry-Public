# Порт сервера (Render/Railway сами задают через переменную PORT)
PORT=3001

# Ограничение размера загружаемого PDF (МБ)
MAX_PDF_MB=30

# --- Забор контрактов из почты (необязательно) ---
# Для Mail.ru: Настройки почты -> Пароль для внешних приложений -> создать новый,
# использовать его ниже (НЕ обычный пароль от почты).
# MAIL_IMAP_HOST=imap.mail.ru
# MAIL_USER=your@mail.ru
# MAIL_APP_PASSWORD=xxxxxxxxxxxxxxxx
# MAIL_FOLDER=INBOX
# MAIL_LOOKBACK_DAYS=60
